# Especifica a imagem base
FROM confluentinc/cp-kafka:7.6.1

# Define o utilizador 'root' como o utilizador padrão para a execução do contentor.
# Isto garante que o "Start Command" terá as permissões necessárias.
USER root
